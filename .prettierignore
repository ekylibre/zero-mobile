node_modules/
ios/
android/
.expo/
dist/
coverage/
web-build/
*.lock
pnpm-lock.yaml

# Artefacts Claude Code (réglages locaux + docs de planification) — hors
# périmètre Prettier / CI. claudedocs contient des blocs de code non idempotents.
.claude/
claudedocs/

# Régénéré par Expo à chaque start Metro sans newline final → boucle
# infinie de "Insert ⏎" sur le lint. Pas la peine de le formater.
expo-env.d.ts
